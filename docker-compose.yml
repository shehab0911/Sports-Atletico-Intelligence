services:
  backend:
    build:
      context: ./backend
    container_name: atletico-backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: atletico-frontend
    depends_on:
      - backend
    ports:
      - "5500:80"
